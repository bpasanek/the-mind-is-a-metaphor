<div class="top_nav"><%= link_to 'Show', admin_religion_path(@religion) %> |
<%= link_to 'Back', admin_religions_path %></div>

<h1>Editing religion</h1>

<% form_for(@religion, :url => {:action => :update}) do |f| %>
  
  <%= render 'form_fields', :f=>f %>
	
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<div class="bottom_nav"><%= link_to 'Show', admin_religion_path(@religion) %> |
<%= link_to 'Back', admin_religions_path %></div>
