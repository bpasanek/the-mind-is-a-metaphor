<div class="top_nav"><%= link_to 'Show', admin_type_path(@type) %> |
<%= link_to 'Back', admin_types_path %></div>

<h1>Editing type</h1>

<% form_for(@type, :url => {:action => :update}) do |f| %>
  
  <%= render 'form_fields', :f=>f %>
	
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<div class="bottom_nav"><%= link_to 'Show', admin_type_path(@type) %> |
<%= link_to 'Back', admin_types_path %></div>
