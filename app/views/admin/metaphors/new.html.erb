<div class="top_nav"><%= link_to 'Back', collection_path %></div>

<h1>New metaphor
	<% if (@work) -%>
	for Work, "<%= h @work.title %>"
	<% end %>
</h1>

<% form_for(setup_metaphor(@metaphor), :url => {:action => :create}) do |f| %>
  
  <%= render 'form_fields', :f=>f %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<div class="bottom_nav"><%= link_to 'Back', collection_path %></div>