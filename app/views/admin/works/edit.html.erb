<div class="top_nav"><%= link_to 'Show', object_path(@work) %> |
<%= link_to 'Back', collection_path %></div>

<h1>Editing work
	<% if (@author) -%>
	by "<%= @author.name %> (<%= @author.date_of_birth %> - <%= @author.date_of_death %>)"
	<% end %>
</h1>

<% form_for(setup_work(@work), :url => object_path) do |f| %>
  
	<%= render 'form_fields', :f=>f %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<div class="bottom_nav"><%= link_to 'Show', object_path(@work) %> |
<%= link_to 'Back', collection_path %></div>
